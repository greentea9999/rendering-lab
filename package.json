{
  "name": "rendering-lab-docs",
  "private": true,
  "type": "module",
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "app:build": "npm --prefix webgpu-ts run build",
    "site:build": "npm run docs:build && npm run app:build && node scripts/copy-demo.mjs"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}